# Frontend environment variables
# Copy to .env.local and adjust as needed

# Set to "true" to use mock data without hitting the Flask backend
VITE_USE_MOCK=true

# Flask backend URL (default: http://localhost:5000)
VITE_API_BASE_URL=http://localhost:5000
